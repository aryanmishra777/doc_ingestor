# Required
playwright==1.58.0
requests==2.32.3
beautifulsoup4==4.12.3
trafilatura==2.0.0

# Optional: PDF support (pip install pymupdf, then no extra setup needed)
pymupdf==1.27.2.3

# Optional: Markdown (.md) support
markdown-it-py==3.0.0

# Optional: LLM-assisted seed discovery (requires OLLAMA_API_KEY in .env)
ollama==0.6.1

# Optional: LangChain adaptive agents (uncomment to enable; see README "Adaptive Agent Runtime")
# langchain
# langchain-ollama
# langchain-huggingface
# langchain-community

# Optional: Intel local runtimes for the adaptive agents
# optimum[openvino]   # DOC_INGESTOR_AGENT_RUNTIME=openvino
# ipex-llm            # DOC_INGESTOR_AGENT_RUNTIME=ipex
