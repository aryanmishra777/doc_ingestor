# Required
playwright==1.58.0
requests==2.32.5
beautifulsoup4==4.12.3
trafilatura==2.0.0

# Optional: PDF support (pip install pymupdf, then no extra setup needed)
pymupdf==1.27.2.3

# Optional: Markdown (.md) support
markdown-it-py==3.0.0

# Optional: LLM-assisted seed discovery (requires OLLAMA_API_KEY in .env)
ollama==0.6.1

# Optional: LangChain adaptive agents (see README "Adaptive Agent Runtime")
# Pinned: unpinned, pip's resolver backtracks for ages hunting a mutually
# compatible set and can land on ancient broken releases.
langchain==1.3.9
langchain-ollama==1.1.0
langchain-huggingface==1.2.2
langchain-community==0.4.2

# Optional: Intel local runtimes for the adaptive agents.
# Install these SEPARATELY (each pins old transformers/tokenizers and fights
# the langchain stack if resolved together):
#   pip install "optimum[openvino]"   # DOC_INGESTOR_AGENT_RUNTIME=openvino
#   pip install ipex-llm              # DOC_INGESTOR_AGENT_RUNTIME=ipex
